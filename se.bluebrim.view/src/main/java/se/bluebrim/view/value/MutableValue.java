package se.bluebrim.view.value;

/**
 * Abstract superclass to value objects. Value objects makes it possible to
 * keep a reference to a boolean, integer etc that change value. Top use Boolean
 * and Integer would not ne poosible because they are immutable and we can't use
 * int or boolean because the are not objects.
 * 
 * @author G Stack
 */
public abstract class MutableValue
{

}
